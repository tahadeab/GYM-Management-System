Playwright UI test run: Electron binary was repaired successfully. The new workflow spec then launched the app, but all four tests stopped at login. Screenshot shows the bilingual landing/login page with the form below the fold; the generic `input[type=text]` selector did not reach a visible login form and the dashboard never appeared. Next action: inspect the actual login HTML selectors and update the fixture.
