# دليل تثبيت نظام إدارة الجيم المتطور

## متطلبات النظام

### الحد الأدنى للمتطلبات:
- **نظام التشغيل**: Windows 10/11، macOS 10.14+، أو Linux Ubuntu 18.04+
- **المعالج**: Intel Core i3 أو AMD Ryzen 3 أو أحدث
- **الذاكرة**: 4 جيجابايت RAM كحد أدنى (8 جيجابايت مُوصى به)
- **مساحة التخزين**: 500 ميجابايت مساحة فارغة
- **الشبكة**: اتصال بالإنترنت (للتحديثات والنسخ الاحتياطي)

### البرامج المطلوبة:
- **Node.js** الإصدار 16.0 أو أحدث
- **npm** (يأتي مع Node.js)

## خطوات التثبيت

### 1. تحميل Node.js
قم بتحميل وتثبيت Node.js من الموقع الرسمي:
- **Windows/Mac**: [https://nodejs.org](https://nodejs.org)
- **Ubuntu/Debian**: 
  ```bash
  sudo apt update
  sudo apt install nodejs npm
  ```
- **CentOS/RHEL**:
  ```bash
  sudo yum install nodejs npm
  ```

### 2. التثبيت التلقائي

#### لأنظمة Windows:
1. انقر نقراً مزدوجاً على ملف `setup.bat`
2. اتبع التعليمات التي تظهر على الشاشة
3. انتظر حتى اكتمال التثبيت

#### لأنظمة Linux/Mac:
1. افتح Terminal في مجلد المشروع
2. نفذ الأمر التالي:
   ```bash
   ./setup.sh
   ```
3. اتبع التعليمات التي تظهر على الشاشة

### 3. التثبيت اليدوي (إذا فشل التثبيت التلقائي)

1. افتح Terminal أو Command Prompt في مجلد المشروع
2. نفذ الأوامر التالية بالترتيب:
   ```bash
   npm install
   node database/improved_db.js
   ```

## تشغيل النظام

### تطبيق سطح المكتب (مُوصى به):
```bash
npm start
```

### خادم الويب:
```bash
npm run web
```
ثم افتح المتصفح وانتقل إلى: `http://localhost:8080/frontend/professional_login.html`

## بيانات تسجيل الدخول الافتراضية

- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

> ⚠️ **تحذير أمني**: يرجى تغيير كلمة المرور فور تسجيل الدخول لأول مرة

## استكشاف الأخطاء وإصلاحها

### مشكلة: "node is not recognized"
**الحل**: تأكد من تثبيت Node.js وإضافته إلى متغير البيئة PATH

### مشكلة: "npm install failed"
**الحل**: 
1. احذف مجلد `node_modules`
2. نفذ `npm cache clean --force`
3. نفذ `npm install` مرة أخرى

### مشكلة: "Database connection failed"
**الحل**: 
1. تأكد من وجود مجلد `database`
2. نفذ `node database/improved_db.js` لإعادة تهيئة قاعدة البيانات

### مشكلة: "Port 8080 is already in use"
**الحل**: 
1. أغلق أي تطبيق يستخدم المنفذ 8080
2. أو غيّر المنفذ في ملف `package.json`

## الدعم الفني

للحصول على المساعدة:
1. راجع ملف `README.md` للمزيد من المعلومات
2. تحقق من ملف `troubleshooting.md` للمشاكل الشائعة
3. تواصل مع فريق الدعم الفني

## معلومات إضافية

- **الإصدار**: 2.0
- **تاريخ الإصدار**: أغسطس 2025
- **الترخيص**: ترخيص تجاري
- **المطور**: taha deab 
